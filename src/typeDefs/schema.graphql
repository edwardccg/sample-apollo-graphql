# import User from './types/User.graphql'
# import Content from './interfaces.graphql'

scalar DateTime
scalar EmailAddress

# Query is the reserved type for query operation
type Query {
  users: [User]
  user(userId: Int!): User
  searchContent(body: String!): [Content]
}
